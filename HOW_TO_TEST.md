# 🧪 如何测试地图生成算法

## 快速开始

### 方法1：使用测试页面（推荐）

1. 启动开发服务器：
```bash
npm run dev
```

2. 在浏览器中打开：
```
http://localhost:5173/test_map_generation.html
```

3. 点击按钮测试：
   - **生成 ACT1 地图 (10层)** - 测试10层地图生成
   - **生成 ACT2 地图 (20层)** - 测试20层地图生成
   - **生成 ACT3 地图 (30层)** - 测试30层地图生成
   - **批量测试 (100次)** - 运行100次生成测试，检查成功率

### 方法2：浏览器控制台测试

1. 打开浏览器控制台（F12）

2. 复制粘贴以下代码：

```javascript
// 导入模块（需要在支持ES6 modules的环境中）
import { generateGridMap } from './src/data/gridMapLayout_v2.js';

// 生成ACT1地图
const map1 = generateGridMap(1);
console.log('ACT1:', map1);

// 生成ACT2地图
const map2 = generateGridMap(2);
console.log('ACT2:', map2);

// 生成ACT3地图
const map3 = generateGridMap(3);
console.log('ACT3:', map3);
```

---

## 测试页面功能说明

### 1. 可视化地图渲染

测试页面会将生成的地图渲染为六边形网格，每个节点用不同颜色表示：

- 🟢 **绿色** - 起点 (START)
- 🔴 **红色** - BOSS
- 🟠 **橙色** - 战斗节点 (BATTLE)
- 🔵 **青色** - 商店 (SHOP)
- 🟡 **黄色** - 事件 (EVENT)
- 🟢 **青绿** - 休息点 (REST)
- 🟣 **紫色** - 宝箱 (CHEST)

连接线显示节点之间的路径关系。

### 2. 统计数据

测试页面会实时显示以下统计数据：

- **当前ACT** - 正在测试的章节
- **总节点数** - 地图中的节点总数
- **最短路径** - 从起点到BOSS的最短步数（BFS计算）
- **最长路径** - 从起点到BOSS的最长步数（DFS计算）
- **BOSS可达** - BOSS是否可达（✅/❌）
- **生成耗时** - 地图生成所需时间（毫秒）

### 3. 批量测试

点击"批量测试"按钮会运行100次地图生成测试，并在控制台输出：

- **成功率** - 成功生成可达地图的百分比
- **平均节点数** - 各ACT的平均节点数
- **平均步数** - 各ACT的平均最短/最长路径步数

**预期成功率**: 100%（算法包含自动修复机制）

---

## 验证标准

### ACT1 (10层)
- ✅ 总节点数: 15-20个
- ✅ 最短路径: 10-15步
- ✅ BOSS 100%可达
- ✅ 每个节点有1-3个next节点
- ✅ 没有孤立节点

### ACT2 (20层)
- ✅ 总节点数: 30-50个
- ✅ 最短路径: 20-40步
- ✅ BOSS 100%可达
- ✅ 每个节点有1-3个next节点
- ✅ 没有孤立节点

### ACT3 (30层)
- ✅ 总节点数: 50-80个
- ✅ 最短路径: 30-60步
- ✅ BOSS 100%可达
- ✅ 每个节点有1-3个next节点
- ✅ 没有孤立节点

---

## 常见问题

### Q1: 地图渲染不出来怎么办？

**A**: 检查浏览器控制台是否有错误。常见原因：
- ES6 modules 支持问题：确保使用现代浏览器（Chrome 89+, Firefox 88+, Edge 89+）
- Vite开发服务器未启动：运行 `npm run dev`

### Q2: 批量测试显示失败怎么办？

**A**: 查看控制台的错误日志，可能的原因：
- 算法BUG：查看具体错误信息，定位问题
- 步数约束过严：调整 `ACT_CONFIG` 中的 `minSteps` 和 `maxSteps`

### Q3: 生成的地图看起来"太密"或"太稀"？

**A**: 调整以下参数：
- `NODE_WEIGHTS`：调整节点类型分布
- `targetNodeCount`（在 `generateBranches` 函数中）：调整目标节点数
- `avgSteps * 1.3`：调整分支密度系数

### Q4: 如何导出地图数据？

**A**: 在控制台中运行：
```javascript
// 获取当前地图数据
const mapData = currentMapData;

// 导出为JSON
const json = JSON.stringify(mapData, null, 2);
console.log(json);

// 复制到剪贴板（需要手动复制）
copy(json); // Chrome DevTools
```

---

## 下一步测试

Phase 1（算法）测试通过后，将进行：

1. **Phase 2: UI组件测试**
   - 测试 HexagonNode.jsx 渲染
   - 测试 GridMapView.jsx 交互
   - 测试迷雾效果和三选一高亮

2. **Phase 3: 集成测试**
   - 测试与 App.jsx 的集成
   - 测试存档/读档功能
   - 测试 ACT1 -> ACT2 -> ACT3 流程

3. **Phase 4: 完整流程测试**
   - 完整游戏流程（选英雄 -> 地图 -> 战斗 -> BOSS）
   - 压力测试（100次完整流程）
   - 性能测试（大地图渲染帧率）

---

## 参考资料

- **算法源码**: `src/data/gridMapLayout_v2.js`
- **六边形工具**: `src/utils/hexagonGrid.js`
- **测试页面**: `test_map_generation.html`
- **规划文档**: `new_grid.md`

---

**更新时间**: 2025-11-21  
**版本**: v0.9.0-alpha.1  
**分支**: new_grid

